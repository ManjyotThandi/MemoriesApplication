let express,bodyParser,mongoose,cors,dotenv,postRoutes;_e14‍.w("express",[["default",["express"],function(v){express=v}]]);_e14‍.w("body-parser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_e14‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_e14‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_e14‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);_e14‍.w("./routes.js/posts",[["default",["postRoutes"],function(v){postRoutes=v}]]);







const app = express();


dotenv.config();

app.use(bodyParser.json({ limit: "30mb", extended: true }));
app.use(bodyParser.urlencoded({ limit: "30mb", extended: true }));
app.use(cors());

// use express middleware to use Router
app.use("/posts", postRoutes);

// MONGO db atlas connection
const CONNECTION_URL = `mongodb+srv://${process.env.MONGO_USERNAME}:${process.env.MONGO_PASSWORD}@cluster0.gaer5.mongodb.net/<dbname>?retryWrites=true&w=majority`
const PORT = process.env.PORt || 5000;

// use mongoose to connect to db
mongoose.connect(CONNECTION_URL, { useNewUrlParser: true, useUnifiedTopology: true })
    .then(() => {
        app.listen(PORT, () => {
            console.log(`Server running on port ${PORT}`)
        })
    })
    .catch((error) => {
        _e14‍.g.console.log(process.env.USERNAME)
        _e14‍.g.console.log(error.message)
    });

// These are so we don't get any warnings in console. This as well as useNewUrlParse and useUnified Topology
//Update, this throws an error so just commenting it out for now
//mongoose.set('useFindandModify', false);